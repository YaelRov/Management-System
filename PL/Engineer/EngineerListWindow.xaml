<Window x:Class="PL.Engineer.EngineerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Engineer"
        mc:Ignorable="d"
        Title="EngineerListWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
        <!--External Grid-->
        <Grid ShowGridLines="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="40*"/>
                <RowDefinition Height="500*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
        <!--Internal, Upper Grid-->
        <Grid HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch"
Width="auto" ShowGridLines="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- Engineers Filter-->
            <Label Content="Sort By Level:" HorizontalAlignment="Stretch" Grid.Column="0" Margin="83,0,0,0" VerticalAlignment="Stretch"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource FilterByEngineerExpCollectionKey}}"
                      SelectedValue="{Binding Experience, Mode=TwoWay}"
                      SelectionChanged="cbEngineerSelector_SelectionChanged"
                      HorizontalAlignment="Stretch" Grid.Column="1" VerticalAlignment="Stretch" />
        </Grid>
        <ListView Margin="5" Grid.Row="1" ItemsSource="{Binding EngineerList}" MouseDoubleClick="ClickOnSingleEng">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Height="50" Width="100"
                Click="onClickAddEng"/>
    </Grid>
</Window>
